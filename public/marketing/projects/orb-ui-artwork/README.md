# Orb UI social artwork

`homepage-social.png` is the 1200×630 gallery image rendered from `design.html`.
It uses the unchanged `homepage-orb.png`, exported from the actual cloud-theme
canvas on https://orb-ui.com/ in manual speaking mode at volume 1, at retina
resolution on September 20, 2026. The orb is not AI-generated. Typography and
layout are composed in HTML/CSS. This replaces the older website screenshot.
